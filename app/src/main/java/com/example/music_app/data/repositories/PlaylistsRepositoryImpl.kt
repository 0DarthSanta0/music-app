package com.example.music_app.data.repositories

import com.example.music_app.domain.repositories.PlaylistsRepository

class PlaylistsRepositoryImpl: PlaylistsRepository {
    override fun createPlaylist(name: String, description: String) {
        super.createPlaylist(name, description)
    }
}